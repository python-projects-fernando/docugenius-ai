# --- DocuGeniusAI Environment Variables Example ---
# 
# This file serves as a template for required environment variables.
# Copy this file to .env.development, .env.production, etc., and fill in the actual values.
# Never commit the filled .env files to version control.
# ---

# --- Initial Admin Setup ---
# Initial admin password for first-time setup (change immediately after first login)
# Example: INITIAL_ADMIN_PASSWORD=MySecurePassword123!
INITIAL_ADMIN_PASSWORD= # <-- Fill this in

# --- JWT Configuration ---
# Secrets for JWT access and refresh tokens (use strong, random secrets)
# Example: JWT_SECRET_KEY=very-long-and-random-string-for-access-tokens
JWT_SECRET_KEY= # <-- Fill this in
# Example: JWT_REFRESH_SECRET_KEY=another-very-long-and-random-string-for-refresh-tokens
JWT_REFRESH_SECRET_KEY= # <-- Fill this in
# Token expiration times
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# --- Database Configuration ---
# SQLAlchemy-compatible database URL (adjust host, user, password, database name)
# Example (MySQL): DATABASE_URL=mysql+aiomysql://user:password@localhost:3306/mydb
DATABASE_URL= # <-- Fill this in (e.g., mysql+aiomysql://user:password@host:port/database)

# --- Hugging Face / AI Configuration ---
# Hugging Face API token for accessing models (if using HF)
# Example: HF_API_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
HF_API_TOKEN= # <-- Fill this in (optional, leave empty if not using HF directly)
# Base URL for Hugging Face OpenAI compatible endpoint (if using HF router)
# Example: HF_OPENAI_BASE_URL=https://router.huggingface.co/v1
HF_OPENAI_BASE_URL= # <-- Fill this in (optional, leave empty if using OpenAI directly)

# --- AWS S3 Configuration ---
# AWS credentials for file storage (if using S3)
# Example: AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_ACCESS_KEY_ID= # <-- Fill this in
# Example: AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_SECRET_ACCESS_KEY= # <-- Fill this in
# S3 bucket name and region for file storage
# Example: AWS_S3_BUCKET_NAME=my-docugenius-bucket
AWS_S3_BUCKET_NAME= # <-- Fill this in
# Example: AWS_S3_REGION=us-east-1
AWS_S3_REGION= # <-- Fill this in
# Choose storage backend: S3 or LOCAL
# Example: STORAGE_BACKEND=S3
STORAGE_BACKEND=S3 # Options: S3, LOCAL

# --- Redis Configuration ---
# Redis server details for caching and temporary data (e.g., reset tokens)
# Example: REDIS_HOST=localhost
REDIS_HOST=localhost # Adjust if Redis is on a different host
# Example: REDIS_PORT=6379
REDIS_PORT=6379
# Example: REDIS_PASSWORD=myredispassword (leave empty if no password)
REDIS_PASSWORD= # <-- Fill this in if Redis requires a password
# Redis database number (default is usually 0)
REDIS_DB=0

# --- Email Configuration (SMTP) ---
# SMTP settings for sending emails (e.g., password reset links)
# Example: SMTP_SERVER=smtp.gmail.com
SMTP_SERVER= # <-- Fill this in (e.g., smtp.gmail.com, smtp.outlook.com)
# Example: SMTP_PORT=587
SMTP_PORT=587 # Common ports: 587 (TLS), 465 (SSL)
# Example: EMAIL_ADDRESS=your-email@domain.com
EMAIL_ADDRESS= # <-- Fill this in
# App password or access token for the email account (not your regular password)
# Example: EMAIL_PASSWORD=your_app_password_or_token
EMAIL_PASSWORD= # <-- Fill this in (use app password for Gmail, etc.)

# --- Application Configuration ---
# Base URL for the application (used in emails, links, etc.)
# Example: BASE_URL=https://api.docugenius-ai.com
BASE_URL= # <-- Fill this in (e.g., http://localhost:8000 for dev, https://api.prod.com for prod)

# --- End of .env.example ---